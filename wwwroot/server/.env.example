# FavsHub Server 环境变量配置

# JWT 签名密钥（生产环境必改）
JWT_SECRET=favshub-secret-change-in-production

# 服务端口（默认 3000）
PORT=3000

# 管理员用户名列表（逗号分隔，会覆盖数据库中的 is_admin 标记）
ADMIN_USERS=admin

# 数据库路径（默认 ./wwwroot/server/data/favshub.db）
# 可通过绝对路径指定，例如：
# DB_PATH=/data/favshub.db
